/** Tên collection MongoDB của module Performance (đặt tiền tố `perf_`). */
export const PERFORMANCE_COLLECTIONS = {
    criteriaSets: "perf_criteria_sets",
    cycles:       "perf_cycles",
    reviews:      "perf_reviews",
} as const;
