/** Tên collection MongoDB của module Setting (đặt tiền tố `set_`). */
export const SETTING_COLLECTIONS = {
    company: "set_company",
    system:  "set_system",
    bankProfiles: "set_bank_profiles",
} as const;
