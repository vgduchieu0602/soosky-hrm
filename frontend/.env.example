# Dev: duong dan TUONG DOI -> vite dev server proxy /api sang http://localhost:3000
# (xem vite.config.ts). Prod: docker build-arg VITE_API_BASE_URL, nginx proxy /api/v1.
# Backend mount router duoi /api/v1 (API_PREFIX trong createExpressServer.ts).
VITE_API_BASE_URL=/api/v1
VITE_APP_NAME=Soosky HRM
